<Window x:Class="GroundhogWindows.Views.Settings.ConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GroundhogWindows"
        mc:Ignorable="d"
        Style="{DynamicResource ResourceKey='Window style'}"
        Title="{DynamicResource ResourceKey=Lang.Settings.ConnectionString}"
        Height="300" Width="519" MinHeight="300" MinWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <TextBox Name="textBoxConnectionStorage" Margin="10,10,10,10" TextWrapping="Wrap" Grid.Row="0"/>
        <TextBox Name="textBoxConnectionLanguage" Margin="10,10,10,10" TextWrapping="Wrap" Grid.Row="1"/>
        <Button Content="{DynamicResource ResourceKey=Lang.ControlCommands.Save}" HorizontalAlignment="Center" Margin="0,7,0,0" Height="35" Width="94" FontSize="16" Click="Button_Click" VerticalAlignment="Top" Grid.Row="2"/>
    </Grid>
</Window>
